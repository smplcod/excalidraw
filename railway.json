{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd excalidraw-app && yarn install --frozen-lockfile && yarn build"
  },
  "deploy": {
    "startCommand": "npx serve -s excalidraw-app/dist -l 8080"
  }
}
